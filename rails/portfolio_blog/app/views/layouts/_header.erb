<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="/"><b>ALEXANDER</b></a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="nav navbar-nav mr-auto">
      <% @section_header.each do |section| %>
      <li class="nav-item">
        <%= link_to "#{section.name}", section, class: "nav-link" %>
      <% end %>
    </ul>
    <% if logged_in? %>
      <div class="form-inline my-2 my-lg-0">
        <%= link_to "Admin Panel", current_user, class: "btn btn-success" %>
        <%= link_to "Logout", admin_abmelden_path, method: :delete, data: {cofirm: "Do you want to log out ?"}, class: "btn btn-danger" %>
      </div>
    <% end %>

  </div>
</nav>
